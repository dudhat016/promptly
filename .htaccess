RewriteEngine On
RewriteBase /

# 1. If the file or directory exists, serve it directly
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# 2. Otherwise, proxy to the Node.js app on Port 3000
RewriteRule ^(.*)$ http://127.0.0.1:3000/$1 [P,L]
